services:
  nextjs-node:
    container_name: nextjs-node__container
    image: node:22
    volumes:
      - type: bind
        source: "./project"
        target: "/var/www/project"
    working_dir: "/var/www/project"
    tty: true
    ports:
      - "3000:3000"
